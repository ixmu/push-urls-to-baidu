dist: xenial
sudo: false
env:
  global:
  - REPO=4edd52ba13541f712c940946586a619db0ced21e
script:
- curl ${SITEMAP_TXT_URL} | head -n 200 > urls.txt
- curl -H 'Content-Type:text/plain' --data-binary @urls.txt "${BAIDU_API}"
